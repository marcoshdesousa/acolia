{
  "name": "acolia",
  "version": "1.0.0",
  "description": "Acolia — plataforma de psicologia online (vitrine de profissionais, chat e atendimento por vídeo)",
  "main": "server/index.js",
  "private": true,
  "scripts": {
    "start": "node --no-warnings server/index.js",
    "dev": "node --no-warnings --watch server/index.js",
    "test": "node --no-warnings --test test/*.test.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.4.0",
    "socket.io": "^4.8.3",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
